# 概念

### 对象 = 属性 + 访问

* 数据：属性或状态
* 操作：函数
* 封装： 把操作和数据放在一起

### 访问控制

`private`

* 只有这个类内部可以访问
* 类内部指类的成员函数和定义初始化
* 这个限制是针对类而言的，不是针对对象的

`public`

* 表示这个类可以用来给其他地方定义变量
* 存储该类的文件名必须和该类类名一样

### 编译单元


* 编译的时候一次对这个单元作为编译单元
* 一个编译单元中有不止一个类的时候，只能有一个是public的

### 包

